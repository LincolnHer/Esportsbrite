import { csrfFetch } from "./csrf.js";

// Actions

// Action Creators

// Thunks

// Reducer
const initialState = {}

export default function ticketsReducer (state = initialState, action) {
    let newState;
    switch(action.type) {
        default:
            return state;
    }
}

import Navigation from "../Navigation"

const EditEventFormPage = (isLoaded) => {
    return (
        <div className="Splash-Home">
            <Navigation isLoaded={isLoaded}/>
            <h1>Hello from Edit Event Page</h1>
        </div>
    )
}

export default EditEventFormPage
